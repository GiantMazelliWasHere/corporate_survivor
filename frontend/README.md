# Frontend

React + Vite + TypeScript application for Corporate Survivor.

The frontend renders the current game state returned by the backend API. It must not hardcode events, scoring, endings, or unlock rules.

Planned folders:

```text
src/
  api/
  components/
  hooks/
  pages/
  styles/
```
