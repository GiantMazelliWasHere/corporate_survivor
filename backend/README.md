# Backend

Node.js + Express + TypeScript API for Corporate Survivor, with SQLite persistence and a configurable game engine.

The backend owns game progression, event selection, choice effects, auto-save, scoring, endings, and ranking.

Planned folders:

```text
src/
  db/
  game/
  routes/
```
